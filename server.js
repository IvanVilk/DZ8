const math = require('./math');

console.log('Привет!')

const result = math.add(10, 1);
console.log('Результат выполнения функции add:', result)

const result2 = math.subtract(1000, 1);
console.log('Результат выполнения функции subtract:', result2)
